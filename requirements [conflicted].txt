# requirements.txt

# Core numerical library
numpy>=1.24

# Plotting
matplotlib>=3.7

# Optimization solver (Gurobi requires separate installation + license)
gurobipy>=10.0

# Optional: For CSV handling TODO: Rausnehmen, falls später doch verwendet
pandas>=2.0


# requirements.txt
# Tested with Python 3.12

# Core numerics
numpy>=1.26,<3

# Plotting (3.8+ hat solide Py3.12-Support)
matplotlib>=3.8

# Optimization solver (install seperately + licence)
gurobipy==12.0.1

# ---- Optional / Dev utilities ----
# pandas wird nur für Debug-/Tabellen-Output verwendet (utils.dprint_all_results_from_pkl).
# Falls du die Debug-Tabellen nutzen willst, kommentiere die nächste Zeile ein:
# pandas>=2.2